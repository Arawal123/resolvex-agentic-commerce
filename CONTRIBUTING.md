# Contributing

Create a focused branch, use conventional commits, add deterministic tests for policy or controller changes, and run `npm run typecheck`, `npm run lint`, `npm run test`, `npm run evaluate`, and `npm run build` before opening a pull request. New operational tools require Zod input validation, explicit permission, structured errors, audit output, idempotency where consequential, and a corresponding verification path.
